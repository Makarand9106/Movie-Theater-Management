#ifndef TICKETS_H
#define TICKETS_H

#include "common.h"

// Displays all tickets for a user
void view_my_tickets(int user_id);

// Cancels a ticket for a user
void cancel_ticket(int user_id);

#endif
